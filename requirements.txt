# WeChat data extraction
pywxdump>=3.0.0

# Data processing
lxml>=5.0.0

# Fine-tuning: CUDA backend (NVIDIA GPUs only)
# Requires CUDA toolkit; do not install on Intel/CPU machines
unsloth
bitsandbytes>=0.43.0

# Fine-tuning: Intel/CPU backend
# intel_extension_for_pytorch must be installed separately — the wheel URL depends
# on your torch version. Follow the matrix at:
#   https://intel.github.io/intel-extension-for-pytorch/
# Quick install example (torch 2.3, XPU/Arc):
#   pip install intel_extension_for_pytorch --extra-index-url https://pytorch-extension.intel.com/release-whl/stable/xpu/us/

# Fine-tuning: shared (all backends)
transformers>=4.45.0
datasets>=2.21.0
trl>=0.11.0
peft>=0.13.0
accelerate>=0.34.0

# RAG
chromadb>=0.5.0
sentence-transformers>=3.0.0

# Serving
gradio>=5.0.0
ollama>=0.3.0

# Utilities
pyyaml>=6.0
tqdm>=4.66.0
